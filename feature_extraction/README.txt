# Feature Extraction

##Usage:

    ./feature_extraction.py IMAGE WIDTH HEIGHT FILTER [FILTER...]

##Parameters:

`IMAGE` : A greyscale bitmap image of a predefined symbols.
`WIDTH` : The number of grid cells wide the image is
`HEIGHT`: The number of grid cells high the image is
`FILTER`: A csv file of even height & width composed of numbers




